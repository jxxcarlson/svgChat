module Config exposing(
  tickInterval,
  playgroundWidth, playgroundHeight, dragOffsetX, dragOffsetY)

playgroundWidth = 500

playgroundHeight = 500

dragOffsetX = 370

dragOffsetY = 50

tickInterval = 1000
